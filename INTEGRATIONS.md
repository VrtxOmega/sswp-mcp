# Integration contracts

See [current operating contract](docs/CURRENT_STATE.md). Use explicit task/session IDs, matching shared directories, and real MCP initialization. Older default-session and Cortex-verdict examples require migration.
